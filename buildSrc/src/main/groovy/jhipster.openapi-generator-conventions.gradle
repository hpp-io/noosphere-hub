plugins {
    id "org.openapi.generator"
}

openApiGenerate {
    generatorName = "spring"
    inputSpec = "$rootDir/src/main/resources/swagger/api.yml".toString()
    outputDir = "$buildDir/openapi".toString()
    apiPackage = "ai.hpp.noosphere.hub.web.api"
    modelPackage = "ai.hpp.noosphere.hub.service.api.dto"
    apiFilesConstrainedTo = [""]
    modelFilesConstrainedTo = [""]
    supportingFilesConstrainedTo = ["ApiUtil.java"]
    configOptions = [delegatePattern: "true", title: "noosphere-hub", useSpringBoot3: "true"]
    validateSpec = true
    importMappings = [Problem:"org.springframework.http.ProblemDetail"]
}

sourceSets {
    main {
        java {
            srcDir file("${project.buildDir.path}/openapi/src/main/java")
        }
    }
}

compileJava.dependsOn("openApiGenerate")
